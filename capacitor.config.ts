import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'E-Commerce-Complete-Ionic-UI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
